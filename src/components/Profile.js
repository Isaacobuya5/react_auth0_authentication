import React, { PropTypes } from 'react';

const Profile = props => {
    return (
        <div>
            Profile Page
        </div>
    );
};

Profile.propTypes = {
    
};

export default Profile;